# Home Assistant Dashboard Card for Pirate Weather
# Copy this into your Lovelace dashboard YAML or card editor
# Requires card-mod for custom styling (install via HACS)
#
# Add the following to your configuration.yaml frontend section and restart Home Assistant:
#
# frontend:
#   extra_module_url:
#     - /hacsfiles/lovelace-card-mod/card-mod.js?hacstag=XXXXXXXXXX
#
# Replace XXXXXXXXXX with the hacstag from your HACS card-mod resource URL.
# Find this in Settings -> Dashboards -> Resources (3-dot menu)

type: markdown
content: >
  {% set condition = states('weather.pirateweather') %}
  {% set icons = {
    'clear-day': '☀️',
    'clear-night': '🌙',
    'cloudy': '☁️',
    'fog': '🌫️',
    'partly-cloudy-day': '⛅',
    'partly-cloudy-night': '☁️',
    'partlycloudy': '⛅',
    'rainy': '🌧️',
    'snowy': '🌨️',
    'snowy-rainy': '🌨️',
    'sunny': '☀️',
    'windy': '💨',
    'lightning': '⛈️',
    'hail': '🌨️'
  } %}
  {{ icons.get(condition, '🌡️') }} {{ ((state_attr('weather.pirateweather', 'temperature') - 32) * 5/9) | round(1) }}°C / {{ state_attr('weather.pirateweather', 'temperature') | int }}°F
card_mod:
  style:
    ha-markdown$: |
      p {
        font-size: 1.8em !important;
        font-weight: normal !important;
        text-align: right !important;
      }
